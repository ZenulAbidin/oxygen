# double slash means "query backend from the same domain"
VITE_BACKEND_HOST=//

# Rootpath for payment UI. Should end with slash
VITE_ROOTPATH=/dashboard/

# Local documentation service started by docker-compose.local.yml
VITE_DOCUMENTATION_URL=http://localhost:8081
